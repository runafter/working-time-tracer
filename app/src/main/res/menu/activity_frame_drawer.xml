<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:title="보기 전환" android:id="@+id/menu_view">
        <menu>
            <item
                android:id="@+id/menu_dashboard"
                android:icon="@drawable/ic_menu_camera"
                android:title="초기화면" />
            <item
                android:id="@+id/menu_in_out_logs"
                android:icon="@drawable/ic_menu_camera"
                android:title="출입기록" />
        </menu>
    </item>
    <item android:title="출입 기록" android:id="@+id/menu_logs">
        <menu>
            <item
                android:id="@+id/monitoring_pattern"
                android:icon="@drawable/ic_menu_camera"
                android:title="알림 감지 패턴 설정" />
            <item
                android:id="@+id/monitoring_start"
                android:icon="@drawable/ic_menu_camera"
                android:title="감지 시작 (감지 중단됨)" />
            <item
                android:id="@+id/monitoring_running"
                android:icon="@drawable/ic_menu_camera"
                android:title="감지중"
                android:visible="false"/>
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="초기화" />
        </menu>
    </item>
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_camera"
            android:icon="@drawable/ic_menu_camera"
            android:title="출입 기록 뷰어 선택"
            android:visible="false"/>
        <item
            android:id="@+id/nav_manage"
            android:icon="@drawable/ic_menu_manage"
            android:title="권한 설정" />
    </group>
</menu>
